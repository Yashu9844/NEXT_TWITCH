// next.config.js (after)
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your configuration here
  images:{
    domains:[ "utfs.io"]
     
    
  }
};

module.exports = nextConfig;
